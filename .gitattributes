# POSIX shell wrappers and installer must keep LF endings, or the shebang
# breaks on macOS / Linux. The .cmd wrappers stay CRLF for Windows.
bin/tsorg   text eol=lf
bin/tstat   text eol=lf
bin/tsdel   text eol=lf
install.sh  text eol=lf
*.sh        text eol=lf
*.cmd       text eol=crlf
